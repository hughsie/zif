# sanity check on evrcmp

local
	kernel;0.3.6-1;i386;meta

remote
	kernel;0.3.6-1;i386;meta
	kernel;0.3-9;i386;meta

transaction
	update
		kernel

result
	kernel;0.3.6-1;i386;meta

config
	archinfo=i386
