# Install a package which has a dep provided by a package with a
# non-numeric version.

local

remote
	zsh-libs;R10-4;i386;meta
	zsh;1.3.1-2;i386;meta
		Requires
			zsh-libs

transaction
	install
		zsh

result
	zsh-libs;R10-4;i386;meta
	zsh;1.3.1-2;i386;meta

config
	archinfo=i386
